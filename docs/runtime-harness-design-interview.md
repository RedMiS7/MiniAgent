# Runtime / Harness 设计访谈

状态：设计讨论中，仅记录已确认决定；尚未形成完整实现契约。

## 问题与当前阶段

当前 AgentLoop 已串联模型与工具，并处理模型流关闭、工具任务取消及模型轮数限制，但成功历史通过完成事件交付，没有独立可查询的 Run 终态。设计目标是补齐运行管理和复用入口，同时复用已有循环及清理能力。

以 ROADMAP.md 的阶段划分为准：P5 Runtime、P6 Harness、P7 Session。旧 docs/p5-work-breakdown.md 使用历史编号，不作为当前阶段归属依据。本次只收敛设计，不标记任何阶段完成。

## 第一轮已确认决定

用户在第一轮回答：`q1 tau-main q2 Harness可复用 Q3 任务应该取消 Q4 A`。

1. 参考用户本地同级目录 `../tau-main` 的实际实现。此前公开资料仅作为补充，不能假定与本地快照相同。
2. Harness 是可复用入口。每次 Run 默认独立；跨轮历史通过显式 Session 共享，不因复用 Harness 而自动继承。
3. 提前停止运行事件消费应取消任务，避免调用方离开后仍隐含执行工具副作用。可靠触发取消的 API 形式、等待清理的时机及与普通展示回调的区别尚待下一轮确认。
4. 第一版选择失败后结束 Run，不自动重试或恢复。是否将单个工具的结构化失败视为 Run 失败尚待明确，不能据此改变当前工具错误回传模型的行为。

## 理由与取舍

独立 Run 让同一入口服务不同任务时不串用历史和取消目标，代价是调用方必须明确选择共享会话。停止消费即取消，避免事件消费结束后仍有后台副作用；实现不能依赖异步生成器的垃圾回收时机。

第一版不自动恢复，可以先验证终态和取消，不需要立即引入恢复点或副作用重放协议。它不撤销已经发生的文件写入，也不意味着每个工具错误都必然终止 Run。

当前 ROADMAP 的 P6 仍包含重试和恢复要求；本轮只确认“第一版”范围，不推定用户已经授权删除后续要求。该范围关系需要后续澄清。

## 相关验收场景（待实现，不代表已通过）

| 场景 | 预期 | 当前验证状态 |
|---|---|---|
| 同一 Harness 顺序执行两个独立任务 | 第二次不继承第一次的消息、计数或终态 | 待实现后使用假模型验证 |
| 消费者在模型输出中退出运行作用域 | 取消活动调用并等待清理，不启动工具；终态仍可查询 | 作用域接口待确认 |
| 消费者在工具执行中退出运行作用域 | 请求取消并等待工具清理，不启动下一工具或模型调用 | 待实现；不承诺回滚副作用 |
| 工具已执行，下一次模型调用异常 | Run 失败结束，不重试模型，不重放工具 | 待实现后用调用计数验证 |
| 展示没有收到终态事件 | Runtime 仍能提供准确的最终状态 | 待实现后验证 |

## 下一轮待决策

- 以明确的运行作用域承载提前退出和清理，还是选择其他消费契约。
- 单工具失败与 Run 失败的区别。
- Run 状态、结束原因和异常如何向调用方交付。
- Harness 首版是否允许多个 Run 同时活动。
- 审批是否进入首版，以及首版与 P6 后续重试/恢复验收的关系。

## 本次验证与限制

只读核对 ROADMAP、AgentLoop 和现有事件类型；新增以上行为验收场景，并对文档执行 Git 差异格式检查。不新增或修改运行代码，不运行功能测试，不宣称 Runtime 已实现。

仍需用户确认后续边界及完整设计，再开始实现。术语记录见根目录 CONTEXT.md。
