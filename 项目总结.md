# MiniAgent 项目总结

## 一、项目定位

MiniAgent 是一个**用于学习的最小单 Agent 项目**，核心目标有三点：

1. 提供一层**统一的模型调用接口**（自有的 `LLMRequest` / `LLMResponse` / `Message` / `ToolDefinition` / `LLMEvent` / `LLMError`），上层 Agent 完全不感知模型提供商，也不接触厂商 SDK 对象或参数。
2. 保持 **Model 与 Tools 可扩展**，Agent Harness 可复用。
3. **按实际需求推进**，不提前搭建没有明确用途的扩展机制。

技术栈：Python 3.10+，依赖极少（`openai==2.45.0`、`jsonschema==4.26.0`、`pydantic==2.13.4`）。

## 二、目录结构

```text
.
├── AGENTS.md              开发规范与架构约束（给 AI/协作者的规则）
├── README.md             安装、CLI 用法、统一协议、适配差异表
├── ROADMAP.md            P0–P7 分阶段路线图与验收标准
├── requirements.txt      三个固定版本依赖
├── cli.py                单次模型调用 CLI（文本/流式/推理选项）
├── tools_cli.py          工具相关 CLI（输出工具定义、工具结果）
├── agent_cli.py          单任务 Agent CLI（流式展示 Agent 过程）
├── miniagent/            项目主包
│   ├── config.py         模型配置校验、密钥从环境变量读取
│   ├── bootstrap.py      依赖组装：create_model / create_tools
│   ├── _validation.py    共用校验辅助
│   ├── models/           统一模型层与各提供商适配器
│   ├── tools/            工具协议、注册表、执行器、工具事件
│   ├── extensions/       可注册的内置工具（files / shell / image）
│   └── agent/            Agent 层：AgentLoop 与 Agent 事件
├── tests/                离线单元测试（8 个测试文件）
└── docs/                 12 份任务文档，重点记录“为什么这样做”
```

## 三、核心架构

```text
Agent / CLI
  → LLM.generate(LLMRequest) / LLM.stream(LLMRequest)
  → OpenAIAdapter / DeepSeekAdapter / BailianAdapter（+ ImageAdapter）
  → OpenAI SDK
  → LLMResponse / LLMEvent，失败抛 LLMError
```

- **models/**：协议定义（types、base、events、errors）与适配器实现，厂商差异（请求入口、`max_output_tokens`、reasoning、temperature、工具定义与结果格式、续接状态、流式事件）全部收敛在适配层。
- **tools/**：`ToolRegistry` 注册工具、`ToolExecutor` 负责参数校验与执行、`base`/`events` 定义工具上下文与执行事件。
- **extensions/**：文件、命令、图片三类内置工具，以 extension 形式注册，新工具接入无需改执行器。
- **agent/**：`AgentLoop` 实现“调用模型 → 执行工具 → 回传结果”的流式循环，含 `max_steps` 轮数上限，负责工具调用 ID 唯一性与配对校验。
- **事件系统**：Agent、LLM、Tool 三层各自独立的 Pydantic 类型 + 固定 `type` 字段，用判别联合（`discriminated union`）表达；运行关联由上层负责，底层事件不携带运行 ID。
- **安全边界**：密钥仅从 `OPENAI_API_KEY` / `DEEPSEEK_API_KEY` / `DASHSCOPE_API_KEY` 读取，不接受命令行传参、不自动加载 `.env`、不硬编码、不记录敏感内容、不自动切换提供商。

## 四、入口与用法

- `cli.py`：统一参数（`--stream`、`--reasoning`、`--max-output-tokens`、`--temperature`、`--system`、`--timeout`、`--max-retries`），退出码区分成功(0)/模型错误(1)/参数错误(2)/用户中断(130)；**不执行工具**。
- `tools_cli.py`：查看工具定义、回传工具结果。
- `agent_cli.py`：运行一次完整 Agent 任务，在 stderr 逐轮展示 `agent_progress`、工具开始/成功/失败/取消等状态，在 stdout 流式输出文本；支持工作区限定与命令开关。

## 五、测试与验证

- 离线测试：`.venv\Scripts\python.exe -m unittest discover -s tests -v`。
- 覆盖 `httpx.MockTransport` 经真实 SDK 的请求序列化、响应解析与 SSE 解析，验证两家提供商的统一请求、工具续接、参数差异、流式拼接、资源清理与错误处理。
- 测试文件：`test_agent_loop.py`、`test_bailian.py`、`test_core_types.py`、`test_events.py`、`test_image.py`、`test_models.py`、`test_tools.py`。
- 现状：**80 项离线测试通过**；不访问真实模型，不代表账号权限、服务可用性或模型实际效果已得到验证。

## 六、路线图进度（P0–P7）

| 阶段 | 内容 | 状态 |
|---|---|---|
| P0 | 项目基础：架构、文档、测试能力 | 已完成 |
| P1 | Model Adapters：多提供商、流式、工具调用与续接 | 已完成 |
| P2 | Tools 与 Extensions：注册、校验、执行、访问限制 | 已完成 |
| P3 | Core Types 与 Events | 部分完成（事件契约已完成；Agent 最终结果与组件交接约定待补齐） |
| P4 | Agent Loop：流式消费、每步提示、工具循环、Agent 事件 | 已完成（含 `agent_cli.py`） |
| P5 | Agent Runtime 与 Agent Harness（状态、步数限制、取消、资源清理、事件汇集） | 待实现 |
| P6 | Session 与 Streaming（多轮对话、会话隔离、CLI 展示） | 待实现 |
| P7 | 按需探索（持久化、上下文压缩、检索/MCP、规划评估、人工介入） | 按需 |

整体判断：**P4 的 CLI 闭环已可用，达到“最小可学习版本”还差 P5（Runtime/Harness）与 P6（Session/Streaming）**。

## 七、设计取舍与限制

- 不提供 `extra_body` 或任意厂商参数透传；已知不支持的组合在本地抛出 `unsupported_feature`，其余交由服务校验并映射为统一错误。
- 当前**不支持**图片输入之外的音频、结构化输出选项、内置托管工具；本项目**不执行工具**，工具的执行与权限校验由上层应用实现。
- 工具访问限制**不等于**操作系统沙箱；步数上限**不等于**时间或费用上限。
- 续接状态 `Message.continuation` 是可序列化的 opaque JSON，上层只保存与回传，不解释、不打印。

## 八、工程规范（AGENTS.md 摘要）

- 每个任务开始前先读 `ROADMAP.md` 确认阶段与验收标准；一次只推进一个范围明确、可独立验证的小任务。
- 使用满足需求的最小实现，避免无依据的抽象与依赖；遵循现有代码风格，不顺手重构无关代码。
- 修改公共接口时同步更新实现、调用方与测试；默认测试不依赖真实模型服务。
- 每个小任务完成后在 `docs/` 新增一份 Markdown，重点解释“为什么这样做”（问题、方案理由、关键取舍）。本目录下已有 12 份此类文档。
