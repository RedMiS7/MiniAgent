# AGENTS.md

## 项目方向

构建一个可扩展的 Agent 项目。

- 上层 Agent 通过统一模型接口调用模型，不感知模型提供商。
- 新功能按实际需求添加，不提前建设无明确用途的扩展机制。

## 架构约束

- 模型服务的调用、协议转换和提供商差异集中在适配层。
- Agent 不感知底层LLM是什么，不同模型差异由底层处理。
- 配置与组件初始化集中管理，通过构造参数传入依赖。
- 保持模型调用、Agent 流程和工具执行的职责清晰。
- Agent tools 以 extensions 的形式注册，保证后续工具轻松接入Agent

## 修改原则

- 修改前阅读相关实现、测试和文档。
- 说明影响实现的必要假设；重要歧义先澄清。
- 使用满足需求的最小实现，避免无依据的抽象和依赖。
- 遵循现有代码风格，不重构无关代码。
- 修改公共接口时，同步更新受影响的实现、调用方和测试。
- 发现无关问题时单独说明，不顺手修改。

## 验证要求

- 根据变更范围运行相关测试或最小有效检查。
- 修复缺陷时，条件允许则先复现，再修复并验证。
- 默认测试不依赖真实模型服务，优先使用模拟响应或假模型。
- 区分模拟测试与真实接口验证，不夸大验证结果。
- 交付时说明改动、验证结果和未解决的限制。

## 安全边界

- 不硬编码、提交或输出真实密钥。
- 默认不记录完整对话及敏感工具输入输出。
- 未经明确授权，不执行破坏性操作或修改生产配置。
- 不自动切换模型提供商。

## 文档维护

- AGENTS.md 只记录持续有效的开发规则与架构约束。
- 功能使用说明放在 README 或对应模块文档。
- 功能计划、实施步骤和进度放在任务清单或 Issue。
- 只有规则、架构边界或验证方式变化时，才更新本文件。
- 更新时替换失效内容，避免不断追加相互矛盾的规则。