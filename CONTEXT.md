# MiniAgent

可复用的单 Agent 系统。以下术语区分一次任务执行、跨轮对话和可重复使用的运行入口。

## Language

**Run**:
一次有明确开始与结束的 Agent 执行；结束后不会因后续任务而重新变为运行中。
_Avoid_: Session、会话

**Agent Runtime**:
一次 Run 的生命周期管理者，负责运行状态、执行限制、取消和资源清理。
_Avoid_: 模型适配器、会话管理器

**Agent Harness**:
组合模型、工具、循环和 Runtime 的可复用运行入口；复用入口本身不表示复用上次任务的对话历史。
_Avoid_: 单次 Run、隐式会话

**Session**:
跨多次 Run 保留对话消息与模型续接状态的会话；共享会话需要显式指定。
_Avoid_: Run、单次执行

**工具失败**:
一次工具调用返回的错误结果，是模型决定下一步时可使用的信息；它本身不表示整个 Run 失败。
_Avoid_: Run 失败

**工具审批**:
在工具执行前对具体调用作出的人工允许或拒绝决定，由 Harness 接入。
_Avoid_: 事后执行通知、整个 Run 的批准
